<!doctype html>
<html>
  <%- include('shared/head') -%>
  <body>
    <%- include('shared/nav') -%>
    <div class='core-content'>
      <%- include('shared/header') -%>
      <div class='body-wrapper'>
        <div class='body-content'>
          <p><strong>Pay Runs:</strong>
          <ul>
            <% for(var i=0; i < payRuns.length; i++) { %>
              <li><%- JSON.stringify(payRuns[i], null, 2).replace(/[{}]/g, '') %></li>
              <br>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </body>
</html>
