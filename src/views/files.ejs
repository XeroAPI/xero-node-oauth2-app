<!doctype html>
<html>
  <%- include('shared/head') -%>
  <body>
    <%- include('shared/nav') -%>
    <div class='core-content'>
      <%- include('shared/header') -%>
      <div class='body-wrapper'>
        <div class='body-content'>
          <h4>Files:</h4>
          <% for(var i=0; i < files.length; i++) { %>
          <p><i><strong><%= files[i].name %></strong></i> - <%= files[i].id %></p>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
