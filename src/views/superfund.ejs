<!doctype html>
<html>
  <%- include('shared/head') -%>
  <body>
    <%- include('shared/nav') -%>
    <div class='core-content'>
      <%- include('shared/header') -%>
      <div class='body-wrapper'>
        <div class='body-content'>
          <p><strong>Super Funds:</strong>
          <ul>
            <% for(var i=0; i < getSuperFunds.length; i++) { %>
              <li><%- JSON.stringify(getSuperFunds[i], null, 2).replace(/[{}]/g, '') %></li>
              <br>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </body>
</html>
