<!doctype html>
<html>
  <%- include('shared/head') -%>
  <body>
    <%- include('shared/nav') -%>
    <div class='core-content'>
      <%- include('shared/header') -%>
      <div class='body-wrapper'>
        <div class='body-content'>
          <p><strong>TimeSheets:</strong>
          <ul>
            <% for(var i=0; i < timeSheets.length; i++) { %>
              <li><%- JSON.stringify(timeSheets[i], null, 2).replace(/[{}]/g, '') %></li>
              <br>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </body>
</html>
